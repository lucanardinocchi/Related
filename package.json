{
  "name": "related",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "src/shared",
    "src/mobile",
    "src/web",
    "src/backend",
    "src/relay"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build:web": "npm run build --workspace=@related/web",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
